# FILESYSTEM
Your root path `/` is the user's project folder.
- `/`: The project files.
- `/agent/`: Your internal data (memory).
- `/skills/`: Available skills.
Do NOT run `pwd` or search for the project using shell commands. You are already inside it at `/`.
